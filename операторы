def numbers(n):
       result = []
       for i in range(1, 21):
           for j in range(1, 21):
               if i != j and n % (i +j) == 0:
                   pair = (i, j)
                   if pair not in result:
                       result.append(pair)
       return result


number = 13
pairs = numbers(number)
result_str = ' '.join(f"{a}{b}" for a, b in pairs)
print(f'Пароль для числа {number}: {result_str}')
